# /orchestrate saved aliases — named dimension presets.
# Invoke with: /orchestrate <task> alias=<name>
# Explicit strategy=/dimension args still override anything an alias sets.
# Add your own: any dimension from SKILL.md is valid under an alias.

aliases:
  # Careful build: adversarial plan, staged execution, panel review
  red-team:
    strategy: adversarial
    review: panel:3
    models: { planner: strongest, worker: sonnet }

  # Cheap bulk execution with Claude reviewing Codex's work — the staged-codex recipe
  # (strategy-xcli.md): per-task model+effort by complexity, empty-diff check every return
  codex-grind:
    strategy: staged
    engine: codex
    review: dual
    effort: high
    models: { reviewer: sonnet }

  # Fast independent fan-out, minimal ceremony
  swarm:
    strategy: parallel
    workers: 4
    review: spec
    isolation: worktree

  # Walk-away (AFK) grind with hard rails
  afk:
    strategy: loop
    trigger: goal
    budget: { cycles: 20, open_prs: 1 }
    review: dual

  # Big-model thinking, small-model doing (Variant A/B; for Variant C — the architect session
  # routing per dispatch — see strategy-advisor.md)
  architect:
    strategy: advisor
    models: { advisor: strongest, worker: sonnet }
